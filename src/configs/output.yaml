defaults:
  - override hydra/job_logging: logging
hydra:
  env:
    exp_time: "${now:%m-%d-%Y}/${now:%H-%M-%S}"
  output_subdir: "."
  run:
    dir: "../experiments/${now:%m-%d-%Y}/${now:%H-%M-%S}"
  job:
    chdir: True
